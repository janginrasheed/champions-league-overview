export enum NavigationPath {
  SEASONS_URL = '/',
  SEASON_URL = '/{{seasonId}}',
  GROUPS_URL = '/{{seasonId}}/groups',
  MATCHES_URL = '/{{seasonId}}/matches',
  CONFIG_URL = '/{{seasonId}}/config',
  ABOUT_URL = '/{{seasonId}}/about',
}
